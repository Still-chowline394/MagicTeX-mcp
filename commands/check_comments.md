---
description: check_comments tool — list the comments I've accepted for you to address
---

Call the `check_comments` MCP tool to fetch the comments I've accepted, as
structured edit instructions. List them back to me grouped by file,
each with its quoted passage and note — but **don't edit anything yet**. If I then
say "address them", make each edit and call `resolve_comment` per comment.
