---
description: show_diff tool — a side-by-side visual diff of the latest change (image)
argument-hint: [checkpoint-sha]
---

Call the `show_diff` MCP tool to show me a side-by-side visual diff (it returns an
image in the chat). If I passed a checkpoint sha in **$ARGUMENTS**, diff that
checkpoint; otherwise show my current uncommitted changes.
