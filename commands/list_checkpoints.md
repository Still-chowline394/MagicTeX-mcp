---
description: list_checkpoints tool — recent checkpoints with their sha, for show_diff
argument-hint: [limit]
---

Call the `list_checkpoints` MCP tool and show me the recent checkpoints (sha, time,
files/lines changed), newest first. Use **$ARGUMENTS** as the limit if given, else
the default. If I then want to see one, tell me I can run `/show_diff <sha>`.
