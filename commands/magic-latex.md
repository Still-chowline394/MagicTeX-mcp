---
description: Open the MagicTeX workspace — compile this paper and show the live preview
---

Render a live preview of the current LaTeX project with MagicTeX: call the
`render_preview` MCP tool. If the preview is already running this just recompiles
and refreshes it. When it finishes, tell me the workspace URL and the page count,
and remind me I can select text on the PDF to leave comments.
